- name: Install and start Nginx
  hosts: your_server
  become: yes

  tasks:
    - name: Update package repositories
      apt:
        update_cache: yes

    - name: Install Nginx
      apt:
        name: nginx
        state: present

    - name: Start Nginx service
      service:
        name: nginx
        state: started
        enabled: yes
